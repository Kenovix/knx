{% extends "::admin_layout.html.twig" %}

{% block menu %}
	{{ knp_menu_render('FarmaciaBundle:Builder:farmaciaMenu') }}
{% endblock %}

{% block rastro %}{{ wo_render_breadcrumbs() }}{% endblock %}

{% block titulo %}Información de la Imv{% endblock %}

{% block msg %}
	{% if app.session.flash('ok') %}
	    <div class="alert alert-success">
	    	<button data-dismiss="alert" class="close" type="button">×</button>
	        <span>{{ app.session.flash('ok') }}</span>
	    </div>
	{% elseif app.session.flash('info') %}
		<div class="alert alert-info">
	    	<button data-dismiss="alert" class="close" type="button">×</button>
	        <span>{{ app.session.flash('info') }}</span>
	    </div>
	{% elseif app.session.flash('error') %}
		<div class="alert alert-error">
	    	<button data-dismiss="alert" class="close" type="button">×</button>
	        <span>{{ app.session.flash('error') }}</span>
	    </div>
	{% endif %}
{% endblock %}

{% block cuerpo %}	    

	<table class="table">
        <tbody>
        	<tr>
                <td><strong>No.</strong></td>
                <td>{{ imv.id }}</td>
            </tr>
            <tr>
                <td><strong>Tipo</strong></td>
                <td>{{ imv.tipoImv }}</td>
            </tr>
            <tr>
                <td><strong>Cod_Facturacion</strong></td>
                <td>{{ imv.codFact }}</td>
            </tr>
            <tr>
                <td><strong>Nombre</strong></td>
                <td>{{ imv.nombre }}</td>
            </tr>
            <tr>
                <td><strong>Cums</strong></td>
                <td>{{ imv.cums }}</td>
            </tr>
            
        </tbody>
    </table>

	<div class="btn-group">
	    <a href="{{ path('imv_edit', {'imv':imv.id}) }}" class="btn btn-primary">Modificar</a>
	    <a href="{{ path('imv_list') }}" class="btn">Volver</a>
    </div>
{% endblock %}
